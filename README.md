# esp32_proj_server
The server-side component of a project involving the ESP32 microcontroller. The goal: to log a notification sent by the controller via an HTTP request after receiving a remote signal
